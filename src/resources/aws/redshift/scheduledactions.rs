// WARNING: generated by kopium - manual changes will be overwritten
// kopium version: 0.21.3

#[allow(unused_imports)]
mod prelude {
    pub use std::collections::BTreeMap;

    pub use k8s_openapi::apimachinery::pkg::apis::meta::v1::Condition;
    pub use kube::CustomResource;
    pub use schemars::JsonSchema;
    pub use serde::{Deserialize, Serialize};
    pub use typed_builder::TypedBuilder;
}
use self::prelude::*;

/// ScheduledActionSpec defines the desired state of ScheduledAction
#[derive(CustomResource, Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
#[kube(
    group = "redshift.aws.upbound.io",
    version = "v1beta2",
    kind = "ScheduledAction",
    plural = "scheduledactions"
)]
#[kube(status = "ScheduledActionStatus")]
pub struct ScheduledActionSpec {
    /// DeletionPolicy specifies what will happen to the underlying external
    /// when this managed resource is deleted - either "Delete" or "Orphan" the
    /// external resource.
    /// This field is planned to be deprecated in favor of the ManagementPolicies
    /// field in a future release. Currently, both could be set independently and
    /// non-default values would be honored if the feature flag is enabled.
    /// See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "deletionPolicy"
    )]
    #[builder(default, setter(strip_option))]
    pub deletion_policy: Option<ScheduledActionDeletionPolicy>,
    #[serde(rename = "forProvider")]
    pub for_provider: ScheduledActionForProvider,
    /// THIS IS A BETA FIELD. It will be honored
    /// unless the Management Policies feature flag is disabled.
    /// InitProvider holds the same fields as ForProvider, with the exception
    /// of Identifier and other resource reference fields. The fields that are
    /// in InitProvider are merged into ForProvider when the resource is created.
    /// The same fields are also added to the terraform ignore_changes hook, to
    /// avoid updating them after creation. This is useful for fields that are
    /// required on creation, but we do not desire to update them after creation,
    /// for example because of an external controller is managing them, like an
    /// autoscaler.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "initProvider"
    )]
    #[builder(default, setter(strip_option))]
    pub init_provider: Option<ScheduledActionInitProvider>,
    /// THIS IS A BETA FIELD. It is on by default but can be opted out
    /// through a Crossplane feature flag.
    /// ManagementPolicies specify the array of actions Crossplane is allowed to
    /// take on the managed and external resources.
    /// This field is planned to replace the DeletionPolicy field in a future
    /// release. Currently, both could be set independently and non-default
    /// values would be honored if the feature flag is enabled. If both are
    /// custom, the DeletionPolicy field will be ignored.
    /// See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
    /// and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "managementPolicies"
    )]
    #[builder(default, setter(strip_option))]
    pub management_policies: Option<Vec<String>>,
    /// ProviderConfigReference specifies how the provider that will be used to
    /// create, observe, update, and delete this managed resource should be
    /// configured.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "providerConfigRef"
    )]
    #[builder(default, setter(strip_option))]
    pub provider_config_ref: Option<ScheduledActionProviderConfigRef>,
    /// PublishConnectionDetailsTo specifies the connection secret config which
    /// contains a name, metadata and a reference to secret store config to
    /// which any connection details for this managed resource should be written.
    /// Connection details frequently include the endpoint, username,
    /// and password required to connect to the managed resource.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "publishConnectionDetailsTo"
    )]
    #[builder(default, setter(strip_option))]
    pub publish_connection_details_to: Option<ScheduledActionPublishConnectionDetailsTo>,
    /// WriteConnectionSecretToReference specifies the namespace and name of a
    /// Secret to which any connection details for this managed resource should
    /// be written. Connection details frequently include the endpoint, username,
    /// and password required to connect to the managed resource.
    /// This field is planned to be replaced in a future release in favor of
    /// PublishConnectionDetailsTo. Currently, both could be set independently
    /// and connection details would be published to both without affecting
    /// each other.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "writeConnectionSecretToRef"
    )]
    #[builder(default, setter(strip_option))]
    pub write_connection_secret_to_ref: Option<ScheduledActionWriteConnectionSecretToRef>,
}

/// ScheduledActionSpec defines the desired state of ScheduledAction
#[derive(Serialize, Deserialize, Clone, Debug, JsonSchema)]
pub enum ScheduledActionDeletionPolicy {
    Orphan,
    Delete,
}

#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionForProvider {
    /// The description of the scheduled action.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub description: Option<String>,
    /// Whether to enable the scheduled action. Default is true .
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub enable: Option<bool>,
    /// The end time in UTC when the schedule is active, in UTC RFC3339 format(for example, YYYY-MM-DDTHH:MM:SSZ).
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "endTime")]
    #[builder(default, setter(strip_option))]
    pub end_time: Option<String>,
    /// The IAM role to assume to run the scheduled action.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "iamRole")]
    #[builder(default, setter(strip_option))]
    pub iam_role: Option<String>,
    /// Reference to a Role in iam to populate iamRole.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "iamRoleRef"
    )]
    #[builder(default, setter(strip_option))]
    pub iam_role_ref: Option<ScheduledActionForProviderIamRoleRef>,
    /// Selector for a Role in iam to populate iamRole.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "iamRoleSelector"
    )]
    #[builder(default, setter(strip_option))]
    pub iam_role_selector: Option<ScheduledActionForProviderIamRoleSelector>,
    /// Region is the region you'd like your resource to be created in.
    pub region: String,
    /// The schedule of action. The schedule is defined format of "at expression" or "cron expression", for example at(2016-03-04T17:27:00) or cron(0 10 ? * MON *). See Scheduled Action for more information.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub schedule: Option<String>,
    /// The start time in UTC when the schedule is active, in UTC RFC3339 format(for example, YYYY-MM-DDTHH:MM:SSZ).
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "startTime")]
    #[builder(default, setter(strip_option))]
    pub start_time: Option<String>,
    /// Target action. Documented below.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "targetAction"
    )]
    #[builder(default, setter(strip_option))]
    pub target_action: Option<ScheduledActionForProviderTargetAction>,
}

/// Reference to a Role in iam to populate iamRole.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionForProviderIamRoleRef {
    /// Name of the referenced object.
    pub name: String,
    /// Policies for referencing.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub policy: Option<ScheduledActionForProviderIamRoleRefPolicy>,
}

/// Policies for referencing.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionForProviderIamRoleRefPolicy {
    /// Resolution specifies whether resolution of this reference is required.
    /// The default is 'Required', which means the reconcile will fail if the
    /// reference cannot be resolved. 'Optional' means this reference will be
    /// a no-op if it cannot be resolved.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub resolution: Option<ScheduledActionForProviderIamRoleRefPolicyResolution>,
    /// Resolve specifies when this reference should be resolved. The default
    /// is 'IfNotPresent', which will attempt to resolve the reference only when
    /// the corresponding field is not present. Use 'Always' to resolve the
    /// reference on every reconcile.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub resolve: Option<ScheduledActionForProviderIamRoleRefPolicyResolve>,
}

/// Policies for referencing.
#[derive(Serialize, Deserialize, Clone, Debug, JsonSchema)]
pub enum ScheduledActionForProviderIamRoleRefPolicyResolution {
    Required,
    Optional,
}

/// Policies for referencing.
#[derive(Serialize, Deserialize, Clone, Debug, JsonSchema)]
pub enum ScheduledActionForProviderIamRoleRefPolicyResolve {
    Always,
    IfNotPresent,
}

/// Selector for a Role in iam to populate iamRole.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionForProviderIamRoleSelector {
    /// MatchControllerRef ensures an object with the same controller reference
    /// as the selecting object is selected.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "matchControllerRef"
    )]
    #[builder(default, setter(strip_option))]
    pub match_controller_ref: Option<bool>,
    /// MatchLabels ensures an object with matching labels is selected.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "matchLabels"
    )]
    #[builder(default, setter(strip_option))]
    pub match_labels: Option<BTreeMap<String, String>>,
    /// Policies for selection.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub policy: Option<ScheduledActionForProviderIamRoleSelectorPolicy>,
}

/// Policies for selection.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionForProviderIamRoleSelectorPolicy {
    /// Resolution specifies whether resolution of this reference is required.
    /// The default is 'Required', which means the reconcile will fail if the
    /// reference cannot be resolved. 'Optional' means this reference will be
    /// a no-op if it cannot be resolved.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub resolution: Option<ScheduledActionForProviderIamRoleSelectorPolicyResolution>,
    /// Resolve specifies when this reference should be resolved. The default
    /// is 'IfNotPresent', which will attempt to resolve the reference only when
    /// the corresponding field is not present. Use 'Always' to resolve the
    /// reference on every reconcile.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub resolve: Option<ScheduledActionForProviderIamRoleSelectorPolicyResolve>,
}

/// Policies for selection.
#[derive(Serialize, Deserialize, Clone, Debug, JsonSchema)]
pub enum ScheduledActionForProviderIamRoleSelectorPolicyResolution {
    Required,
    Optional,
}

/// Policies for selection.
#[derive(Serialize, Deserialize, Clone, Debug, JsonSchema)]
pub enum ScheduledActionForProviderIamRoleSelectorPolicyResolve {
    Always,
    IfNotPresent,
}

/// Target action. Documented below.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionForProviderTargetAction {
    /// An action that runs a PauseCluster API operation. Documented below.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "pauseCluster"
    )]
    #[builder(default, setter(strip_option))]
    pub pause_cluster: Option<ScheduledActionForProviderTargetActionPauseCluster>,
    /// An action that runs a ResizeCluster API operation. Documented below.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "resizeCluster"
    )]
    #[builder(default, setter(strip_option))]
    pub resize_cluster: Option<ScheduledActionForProviderTargetActionResizeCluster>,
    /// An action that runs a ResumeCluster API operation. Documented below.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "resumeCluster"
    )]
    #[builder(default, setter(strip_option))]
    pub resume_cluster: Option<ScheduledActionForProviderTargetActionResumeCluster>,
}

/// An action that runs a PauseCluster API operation. Documented below.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionForProviderTargetActionPauseCluster {
    /// The identifier of the cluster to be paused.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "clusterIdentifier"
    )]
    #[builder(default, setter(strip_option))]
    pub cluster_identifier: Option<String>,
}

/// An action that runs a ResizeCluster API operation. Documented below.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionForProviderTargetActionResizeCluster {
    /// A boolean value indicating whether the resize operation is using the classic resize process. Default: false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub classic: Option<bool>,
    /// The unique identifier for the cluster to resize.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "clusterIdentifier"
    )]
    #[builder(default, setter(strip_option))]
    pub cluster_identifier: Option<String>,
    /// The new cluster type for the specified cluster.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "clusterType"
    )]
    #[builder(default, setter(strip_option))]
    pub cluster_type: Option<String>,
    /// The new node type for the nodes you are adding.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "nodeType")]
    #[builder(default, setter(strip_option))]
    pub node_type: Option<String>,
    /// The new number of nodes for the cluster.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "numberOfNodes"
    )]
    #[builder(default, setter(strip_option))]
    pub number_of_nodes: Option<f64>,
}

/// An action that runs a ResumeCluster API operation. Documented below.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionForProviderTargetActionResumeCluster {
    /// The identifier of the cluster to be resumed.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "clusterIdentifier"
    )]
    #[builder(default, setter(strip_option))]
    pub cluster_identifier: Option<String>,
}

/// THIS IS A BETA FIELD. It will be honored
/// unless the Management Policies feature flag is disabled.
/// InitProvider holds the same fields as ForProvider, with the exception
/// of Identifier and other resource reference fields. The fields that are
/// in InitProvider are merged into ForProvider when the resource is created.
/// The same fields are also added to the terraform ignore_changes hook, to
/// avoid updating them after creation. This is useful for fields that are
/// required on creation, but we do not desire to update them after creation,
/// for example because of an external controller is managing them, like an
/// autoscaler.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionInitProvider {
    /// The description of the scheduled action.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub description: Option<String>,
    /// Whether to enable the scheduled action. Default is true .
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub enable: Option<bool>,
    /// The end time in UTC when the schedule is active, in UTC RFC3339 format(for example, YYYY-MM-DDTHH:MM:SSZ).
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "endTime")]
    #[builder(default, setter(strip_option))]
    pub end_time: Option<String>,
    /// The IAM role to assume to run the scheduled action.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "iamRole")]
    #[builder(default, setter(strip_option))]
    pub iam_role: Option<String>,
    /// Reference to a Role in iam to populate iamRole.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "iamRoleRef"
    )]
    #[builder(default, setter(strip_option))]
    pub iam_role_ref: Option<ScheduledActionInitProviderIamRoleRef>,
    /// Selector for a Role in iam to populate iamRole.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "iamRoleSelector"
    )]
    #[builder(default, setter(strip_option))]
    pub iam_role_selector: Option<ScheduledActionInitProviderIamRoleSelector>,
    /// The schedule of action. The schedule is defined format of "at expression" or "cron expression", for example at(2016-03-04T17:27:00) or cron(0 10 ? * MON *). See Scheduled Action for more information.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub schedule: Option<String>,
    /// The start time in UTC when the schedule is active, in UTC RFC3339 format(for example, YYYY-MM-DDTHH:MM:SSZ).
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "startTime")]
    #[builder(default, setter(strip_option))]
    pub start_time: Option<String>,
    /// Target action. Documented below.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "targetAction"
    )]
    #[builder(default, setter(strip_option))]
    pub target_action: Option<ScheduledActionInitProviderTargetAction>,
}

/// Reference to a Role in iam to populate iamRole.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionInitProviderIamRoleRef {
    /// Name of the referenced object.
    pub name: String,
    /// Policies for referencing.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub policy: Option<ScheduledActionInitProviderIamRoleRefPolicy>,
}

/// Policies for referencing.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionInitProviderIamRoleRefPolicy {
    /// Resolution specifies whether resolution of this reference is required.
    /// The default is 'Required', which means the reconcile will fail if the
    /// reference cannot be resolved. 'Optional' means this reference will be
    /// a no-op if it cannot be resolved.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub resolution: Option<ScheduledActionInitProviderIamRoleRefPolicyResolution>,
    /// Resolve specifies when this reference should be resolved. The default
    /// is 'IfNotPresent', which will attempt to resolve the reference only when
    /// the corresponding field is not present. Use 'Always' to resolve the
    /// reference on every reconcile.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub resolve: Option<ScheduledActionInitProviderIamRoleRefPolicyResolve>,
}

/// Policies for referencing.
#[derive(Serialize, Deserialize, Clone, Debug, JsonSchema)]
pub enum ScheduledActionInitProviderIamRoleRefPolicyResolution {
    Required,
    Optional,
}

/// Policies for referencing.
#[derive(Serialize, Deserialize, Clone, Debug, JsonSchema)]
pub enum ScheduledActionInitProviderIamRoleRefPolicyResolve {
    Always,
    IfNotPresent,
}

/// Selector for a Role in iam to populate iamRole.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionInitProviderIamRoleSelector {
    /// MatchControllerRef ensures an object with the same controller reference
    /// as the selecting object is selected.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "matchControllerRef"
    )]
    #[builder(default, setter(strip_option))]
    pub match_controller_ref: Option<bool>,
    /// MatchLabels ensures an object with matching labels is selected.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "matchLabels"
    )]
    #[builder(default, setter(strip_option))]
    pub match_labels: Option<BTreeMap<String, String>>,
    /// Policies for selection.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub policy: Option<ScheduledActionInitProviderIamRoleSelectorPolicy>,
}

/// Policies for selection.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionInitProviderIamRoleSelectorPolicy {
    /// Resolution specifies whether resolution of this reference is required.
    /// The default is 'Required', which means the reconcile will fail if the
    /// reference cannot be resolved. 'Optional' means this reference will be
    /// a no-op if it cannot be resolved.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub resolution: Option<ScheduledActionInitProviderIamRoleSelectorPolicyResolution>,
    /// Resolve specifies when this reference should be resolved. The default
    /// is 'IfNotPresent', which will attempt to resolve the reference only when
    /// the corresponding field is not present. Use 'Always' to resolve the
    /// reference on every reconcile.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub resolve: Option<ScheduledActionInitProviderIamRoleSelectorPolicyResolve>,
}

/// Policies for selection.
#[derive(Serialize, Deserialize, Clone, Debug, JsonSchema)]
pub enum ScheduledActionInitProviderIamRoleSelectorPolicyResolution {
    Required,
    Optional,
}

/// Policies for selection.
#[derive(Serialize, Deserialize, Clone, Debug, JsonSchema)]
pub enum ScheduledActionInitProviderIamRoleSelectorPolicyResolve {
    Always,
    IfNotPresent,
}

/// Target action. Documented below.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionInitProviderTargetAction {
    /// An action that runs a PauseCluster API operation. Documented below.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "pauseCluster"
    )]
    #[builder(default, setter(strip_option))]
    pub pause_cluster: Option<ScheduledActionInitProviderTargetActionPauseCluster>,
    /// An action that runs a ResizeCluster API operation. Documented below.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "resizeCluster"
    )]
    #[builder(default, setter(strip_option))]
    pub resize_cluster: Option<ScheduledActionInitProviderTargetActionResizeCluster>,
    /// An action that runs a ResumeCluster API operation. Documented below.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "resumeCluster"
    )]
    #[builder(default, setter(strip_option))]
    pub resume_cluster: Option<ScheduledActionInitProviderTargetActionResumeCluster>,
}

/// An action that runs a PauseCluster API operation. Documented below.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionInitProviderTargetActionPauseCluster {
    /// The identifier of the cluster to be paused.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "clusterIdentifier"
    )]
    #[builder(default, setter(strip_option))]
    pub cluster_identifier: Option<String>,
}

/// An action that runs a ResizeCluster API operation. Documented below.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionInitProviderTargetActionResizeCluster {
    /// A boolean value indicating whether the resize operation is using the classic resize process. Default: false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub classic: Option<bool>,
    /// The unique identifier for the cluster to resize.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "clusterIdentifier"
    )]
    #[builder(default, setter(strip_option))]
    pub cluster_identifier: Option<String>,
    /// The new cluster type for the specified cluster.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "clusterType"
    )]
    #[builder(default, setter(strip_option))]
    pub cluster_type: Option<String>,
    /// The new node type for the nodes you are adding.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "nodeType")]
    #[builder(default, setter(strip_option))]
    pub node_type: Option<String>,
    /// The new number of nodes for the cluster.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "numberOfNodes"
    )]
    #[builder(default, setter(strip_option))]
    pub number_of_nodes: Option<f64>,
}

/// An action that runs a ResumeCluster API operation. Documented below.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionInitProviderTargetActionResumeCluster {
    /// The identifier of the cluster to be resumed.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "clusterIdentifier"
    )]
    #[builder(default, setter(strip_option))]
    pub cluster_identifier: Option<String>,
}

/// ProviderConfigReference specifies how the provider that will be used to
/// create, observe, update, and delete this managed resource should be
/// configured.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionProviderConfigRef {
    /// Name of the referenced object.
    pub name: String,
    /// Policies for referencing.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub policy: Option<ScheduledActionProviderConfigRefPolicy>,
}

/// Policies for referencing.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionProviderConfigRefPolicy {
    /// Resolution specifies whether resolution of this reference is required.
    /// The default is 'Required', which means the reconcile will fail if the
    /// reference cannot be resolved. 'Optional' means this reference will be
    /// a no-op if it cannot be resolved.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub resolution: Option<ScheduledActionProviderConfigRefPolicyResolution>,
    /// Resolve specifies when this reference should be resolved. The default
    /// is 'IfNotPresent', which will attempt to resolve the reference only when
    /// the corresponding field is not present. Use 'Always' to resolve the
    /// reference on every reconcile.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub resolve: Option<ScheduledActionProviderConfigRefPolicyResolve>,
}

/// Policies for referencing.
#[derive(Serialize, Deserialize, Clone, Debug, JsonSchema)]
pub enum ScheduledActionProviderConfigRefPolicyResolution {
    Required,
    Optional,
}

/// Policies for referencing.
#[derive(Serialize, Deserialize, Clone, Debug, JsonSchema)]
pub enum ScheduledActionProviderConfigRefPolicyResolve {
    Always,
    IfNotPresent,
}

/// PublishConnectionDetailsTo specifies the connection secret config which
/// contains a name, metadata and a reference to secret store config to
/// which any connection details for this managed resource should be written.
/// Connection details frequently include the endpoint, username,
/// and password required to connect to the managed resource.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionPublishConnectionDetailsTo {
    /// SecretStoreConfigRef specifies which secret store config should be used
    /// for this ConnectionSecret.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "configRef")]
    #[builder(default, setter(strip_option))]
    pub config_ref: Option<ScheduledActionPublishConnectionDetailsToConfigRef>,
    /// Metadata is the metadata for connection secret.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub metadata: Option<ScheduledActionPublishConnectionDetailsToMetadata>,
    /// Name is the name of the connection secret.
    pub name: String,
}

/// SecretStoreConfigRef specifies which secret store config should be used
/// for this ConnectionSecret.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionPublishConnectionDetailsToConfigRef {
    /// Name of the referenced object.
    pub name: String,
    /// Policies for referencing.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub policy: Option<ScheduledActionPublishConnectionDetailsToConfigRefPolicy>,
}

/// Policies for referencing.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionPublishConnectionDetailsToConfigRefPolicy {
    /// Resolution specifies whether resolution of this reference is required.
    /// The default is 'Required', which means the reconcile will fail if the
    /// reference cannot be resolved. 'Optional' means this reference will be
    /// a no-op if it cannot be resolved.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub resolution: Option<ScheduledActionPublishConnectionDetailsToConfigRefPolicyResolution>,
    /// Resolve specifies when this reference should be resolved. The default
    /// is 'IfNotPresent', which will attempt to resolve the reference only when
    /// the corresponding field is not present. Use 'Always' to resolve the
    /// reference on every reconcile.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub resolve: Option<ScheduledActionPublishConnectionDetailsToConfigRefPolicyResolve>,
}

/// Policies for referencing.
#[derive(Serialize, Deserialize, Clone, Debug, JsonSchema)]
pub enum ScheduledActionPublishConnectionDetailsToConfigRefPolicyResolution {
    Required,
    Optional,
}

/// Policies for referencing.
#[derive(Serialize, Deserialize, Clone, Debug, JsonSchema)]
pub enum ScheduledActionPublishConnectionDetailsToConfigRefPolicyResolve {
    Always,
    IfNotPresent,
}

/// Metadata is the metadata for connection secret.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionPublishConnectionDetailsToMetadata {
    /// Annotations are the annotations to be added to connection secret.
    /// - For Kubernetes secrets, this will be used as "metadata.annotations".
    /// - It is up to Secret Store implementation for others store types.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub annotations: Option<BTreeMap<String, String>>,
    /// Labels are the labels/tags to be added to connection secret.
    /// - For Kubernetes secrets, this will be used as "metadata.labels".
    /// - It is up to Secret Store implementation for others store types.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub labels: Option<BTreeMap<String, String>>,
    /// Type is the SecretType for the connection secret.
    /// - Only valid for Kubernetes Secret Stores.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "type")]
    #[builder(default, setter(strip_option))]
    pub r#type: Option<String>,
}

/// WriteConnectionSecretToReference specifies the namespace and name of a
/// Secret to which any connection details for this managed resource should
/// be written. Connection details frequently include the endpoint, username,
/// and password required to connect to the managed resource.
/// This field is planned to be replaced in a future release in favor of
/// PublishConnectionDetailsTo. Currently, both could be set independently
/// and connection details would be published to both without affecting
/// each other.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionWriteConnectionSecretToRef {
    /// Name of the secret.
    pub name: String,
    /// Namespace of the secret.
    pub namespace: String,
}

/// ScheduledActionStatus defines the observed state of ScheduledAction.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionStatus {
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "atProvider"
    )]
    #[builder(default, setter(strip_option))]
    pub at_provider: Option<ScheduledActionStatusAtProvider>,
    /// Conditions of the resource.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub conditions: Option<Vec<Condition>>,
    /// ObservedGeneration is the latest metadata.generation
    /// which resulted in either a ready state, or stalled due to error
    /// it can not recover from without human intervention.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "observedGeneration"
    )]
    #[builder(default, setter(strip_option))]
    pub observed_generation: Option<i64>,
}

#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionStatusAtProvider {
    /// The description of the scheduled action.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub description: Option<String>,
    /// Whether to enable the scheduled action. Default is true .
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub enable: Option<bool>,
    /// The end time in UTC when the schedule is active, in UTC RFC3339 format(for example, YYYY-MM-DDTHH:MM:SSZ).
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "endTime")]
    #[builder(default, setter(strip_option))]
    pub end_time: Option<String>,
    /// The IAM role to assume to run the scheduled action.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "iamRole")]
    #[builder(default, setter(strip_option))]
    pub iam_role: Option<String>,
    /// The Redshift Scheduled Action name.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub id: Option<String>,
    /// The schedule of action. The schedule is defined format of "at expression" or "cron expression", for example at(2016-03-04T17:27:00) or cron(0 10 ? * MON *). See Scheduled Action for more information.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub schedule: Option<String>,
    /// The start time in UTC when the schedule is active, in UTC RFC3339 format(for example, YYYY-MM-DDTHH:MM:SSZ).
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "startTime")]
    #[builder(default, setter(strip_option))]
    pub start_time: Option<String>,
    /// Target action. Documented below.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "targetAction"
    )]
    #[builder(default, setter(strip_option))]
    pub target_action: Option<ScheduledActionStatusAtProviderTargetAction>,
}

/// Target action. Documented below.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionStatusAtProviderTargetAction {
    /// An action that runs a PauseCluster API operation. Documented below.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "pauseCluster"
    )]
    #[builder(default, setter(strip_option))]
    pub pause_cluster: Option<ScheduledActionStatusAtProviderTargetActionPauseCluster>,
    /// An action that runs a ResizeCluster API operation. Documented below.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "resizeCluster"
    )]
    #[builder(default, setter(strip_option))]
    pub resize_cluster: Option<ScheduledActionStatusAtProviderTargetActionResizeCluster>,
    /// An action that runs a ResumeCluster API operation. Documented below.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "resumeCluster"
    )]
    #[builder(default, setter(strip_option))]
    pub resume_cluster: Option<ScheduledActionStatusAtProviderTargetActionResumeCluster>,
}

/// An action that runs a PauseCluster API operation. Documented below.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionStatusAtProviderTargetActionPauseCluster {
    /// The identifier of the cluster to be paused.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "clusterIdentifier"
    )]
    #[builder(default, setter(strip_option))]
    pub cluster_identifier: Option<String>,
}

/// An action that runs a ResizeCluster API operation. Documented below.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionStatusAtProviderTargetActionResizeCluster {
    /// A boolean value indicating whether the resize operation is using the classic resize process. Default: false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[builder(default, setter(strip_option))]
    pub classic: Option<bool>,
    /// The unique identifier for the cluster to resize.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "clusterIdentifier"
    )]
    #[builder(default, setter(strip_option))]
    pub cluster_identifier: Option<String>,
    /// The new cluster type for the specified cluster.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "clusterType"
    )]
    #[builder(default, setter(strip_option))]
    pub cluster_type: Option<String>,
    /// The new node type for the nodes you are adding.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "nodeType")]
    #[builder(default, setter(strip_option))]
    pub node_type: Option<String>,
    /// The new number of nodes for the cluster.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "numberOfNodes"
    )]
    #[builder(default, setter(strip_option))]
    pub number_of_nodes: Option<f64>,
}

/// An action that runs a ResumeCluster API operation. Documented below.
#[derive(Serialize, Deserialize, Clone, Debug, TypedBuilder, JsonSchema)]
pub struct ScheduledActionStatusAtProviderTargetActionResumeCluster {
    /// The identifier of the cluster to be resumed.
    #[serde(
        default,
        skip_serializing_if = "Option::is_none",
        rename = "clusterIdentifier"
    )]
    #[builder(default, setter(strip_option))]
    pub cluster_identifier: Option<String>,
}
